services:
  foundry-mcp:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file: .env
    restart: unless-stopped
    ports:
      - "127.0.0.1:3210:3210"
    volumes:
      - ./data:/app/data
